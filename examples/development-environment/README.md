# Local Development

In the case you want to regenerate a new docker image for testing purpose, is it possible to use these file to build the image at each launch.

Copy/paste these file into the example to make it work.

Note that it needs both project in the same folder.